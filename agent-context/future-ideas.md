# Future Ideas

This is not an active roadmap. Use it only as a last resort when current todos are depleted and the user asks for what to do next.

- Replace starter content with the first GlobalPayTo public-facing flow.
- Decide whether this repo stays linked to local Cubid workspace packages or moves to published package versions for portable CI.
- Add docs pages once the SDK contracts stabilize.

